<manifest xmlns:android="http://schemas.android.com/apk/res/android"> 

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>

    <application android:label="Polypath Injector" android:exported="true">

        <receiver android:name=".MockReceiver" android:exported="true">
            <intent-filter>
                <action android:name="send.mock"/>
            </intent-filter>
        </receiver>

        <service android:name=".InjectorService" android:exported="false"/>

    </application>
</manifest>
